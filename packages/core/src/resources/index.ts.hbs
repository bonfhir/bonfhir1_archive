{{#fhirPath data.[profiles-resources] "Bundle.entry.select(resource).ofType(StructureDefinition)"}}
export * from "./{{name}}";
{{#writeFiles "{{name}}.ts"}}
import { {{name}} } from "fhir/r4";

export const build{{name}} = (resource: Omit<{{name}}, "resourceType">) => ({
  ...resource,
  resourceType: "{{name}}"
});
{{/writeFiles}}
{{/fhirPath}}
